# Omni-Weather: Radar Nowcasting generation config

radar_gen:
  dataset_names:
  - nowcast_sevir_png
  image_transform_args:
    image_stride: 16
    max_image_size: 256
    min_image_size: 256
  is_mandatory: true
  num_used_data:
  - -1
  weight: 1.0
